# Content ownership

Authoritative sample curricula live in server/content.ts and are seeded into PostgreSQL. The browser receives content through /api; answer keys never live in the frontend bundle. This directory is reserved for future non-sensitive display metadata.
